// I'm using printf()s instead of couts because i try to get used to it. It works faster than couts and cins
// try putting this: ios_base::sync_with_stdio(0); at the beggining of your code. It replaces couts with printf()s
// same thing with scanf() and cin


int main() {




	return 0;
}
